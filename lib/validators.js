import { z } from 'zod';
export const paymentSchema = z.object({ amount:z.coerce.number().positive(), currency:z.string().min(1).max(10), paymentMethod:z.string().min(2).max(40), transactionHash:z.string().trim().min(1).max(200), paymentProof:z.string().url().optional().or(z.literal('')), note:z.string().max(1000).optional() });
export const jobSchema = z.object({ title:z.string().min(3).max(140), description:z.string().min(20), company:z.string().min(2), location:z.string().min(2), jobType:z.string(), salary:z.string().max(80), category:z.string().min(2), requirements:z.array(z.string()).default([]), responsibilities:z.array(z.string()).default([]), skills:z.array(z.string()).default([]), applicationDeadline:z.coerce.date().optional(), status:z.enum(['ACTIVE','CLOSED','DRAFT']).default('DRAFT') });
export const applicationSchema = z.object({ jobId:z.string(), coverLetter:z.string().min(30).max(5000), resume:z.string().url().optional().or(z.literal('')) });
