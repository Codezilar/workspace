import Link from 'next/link'; import {MapPin,ArrowUpRight} from 'lucide-react'; import Badge from '@/components/ui/Badge';
export default function JobCard({job}){return <Link href={`/jobs/${job._id}`} className="glass group block rounded-3xl p-5 transition hover:-translate-y-1 hover:border-acid/40"><div className="flex justify-between"><Badge tone="blue">{job.category}</Badge><ArrowUpRight size={18} className="text-mist transition group-hover:text-acid"/></div><h3 className="mt-5 text-xl font-semibold">{job.title}</h3><p className="mt-2 text-sm text-mist">{job.company} · {job.jobType}</p><p className="mt-2 flex items-center gap-1 text-sm text-mist"><MapPin size={14}/>{job.location}</p><p className="mt-4 font-medium text-acid">{job.salary || 'Compensation discussed on offer'}</p><div className="mt-5 flex flex-wrap gap-2">{job.skills?.slice(0,3).map(x=><Badge key={x}>{x}</Badge>)}</div></Link>}
