'use client';import {useState} from 'react';import {useRouter} from 'next/navigation';export default function ReviewPayment({id}){const [loading,setLoading]=useState(false),r=useRouter();const review=async status=>{setLoading(true);await fetch(`/api/payments/${id}`,{method:'PATCH',headers:{'Content-Type':'application/json'},body:JSON.stringify({status})});r.refresh();setLoading(false)};return <div className="flex gap-2"><button disabled={loading} onClick={()=>review('APPROVED')} className="btn btn-primary px-3 py-2 text-xs">Approve</button><button disabled={loading} onClick={()=>review('REJECTED')} className="btn btn-secondary px-3 py-2 text-xs">Reject</button></div>}
